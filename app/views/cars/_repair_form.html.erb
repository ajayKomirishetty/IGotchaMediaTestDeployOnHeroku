<%= form_with(model: repair) do |form| %>
  <% if repair.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(repair.errors.count, "error") %> prohibited this repair from being saved:</h2>

      <ul>
        <% repair.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= form.label :car_id %>
    <%= form.text_field :car_id, :readonly => true %>
  </div>

   <div class="field">
    <%= form.label :repair_date %>
    <%= form.datetime_select :repair_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
